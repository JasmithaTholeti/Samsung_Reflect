# Database
MONGO_URI=mongodb+srv://rohith57878:12345@cluster0.cgbnqss.mongodb.net/journal?retryWrites=true&w=majority&appName=Cluster0

# Server Configuration
PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
UPLOAD_DIR=./uploads

# AI/ML Configuration
SERVER_INFERENCE=true
MODEL_DIR=./models
MIN_DETECTION_SCORE=0.25
NMS_IOU_THRESHOLD=0.45

# Vector Database (File-based, no external service needed)
VECTOR_STORAGE_PATH=./data/vector_index.json

# ML Service Configuration
ML_SERVICE_URL=http://localhost:8000
ENABLE_OBJECT_DETECTION=true
ENABLE_SCENE_CLASSIFICATION=true
ENABLE_CLIP_EMBEDDINGS=true

# Job Queue (In-memory, no Redis needed)
ENABLE_JOB_QUEUE=true
MAX_CONCURRENT_JOBS=3

# Storage (optional - for production)
S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=

# Feature Flags
ENABLE_CLIENT_INFERENCE=false
ENABLE_RERANKING=true

# Ranking Configuration
OBJECT_WEIGHT=0.7
SCENE_WEIGHT=0.3
DETECTION_SCORE_WEIGHT=0.5
AGGREGATION_METHOD=weighted

# Upload Configuration
MAX_FILE_SIZE=100MB
ALLOWED_MIME_TYPES=image/jpeg,image/png,image/webp

# Redis (for job queue)
REDIS_URL=redis://localhost:6379

# Python Microservice (if using Option B)
PYTHON_SERVICE_URL=http://localhost:8000
